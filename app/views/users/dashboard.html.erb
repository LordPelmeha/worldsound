<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="worldmusic.css">
  <link rel="icon" href="images/favicon.ico" type="image/x-icon">
  <%= stylesheet_link_tag 'dashboard', media: 'all' %>
  <title>Личный кабинет</title>
</head>
<body>
  <div class="container">
    <div class="user-info">
    <%# <h2>Добро пожаловать, <%= user['name'] %>
      <p>Email: <%= user['email'] %></p>
    </div>

    <div class="messages">
      <h3>Ваши сообщения:</h3>
      <% messages.each do |message| %>
        <div class="message-item" onclick="alert('<%= message['content'] %>')">
          <span class="message-topic">Тема: <%= message['topic'] %></span>
          <span class="message-sender">От: <%= message['sender'] %></span>
        </div>
      <% end %>
    </div>
  </div>
</body>
</html>
 <!-- Верхняя часть с данными пользователя -->
    <%# <div class="user-info">
      <h2><%= @user.name %</h2>
      <p>Email: <%= @user.email %</p>
    </div>

    <!-- Нижняя часть с сообщениями -->
    <div class="messages">
      <% @messages.each do |message| %
        <div class="message-item" onclick="location.href='<%= message_path(message) %'">
          <span class="message-topic"><%= message.topic %</span>
          <span class="message-sender">От: <%= message.sender_name %</span>
        </div>
      <% end %
    </div> %>